<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <form action="/" method="post">
        <div>
            <label>Enter the requirement of job or intrnship role:</label>
            <input type="text" name="require" placeholder="Enter Data" >
        </div>
        <button type="submit" >Submit</button>
    </form>
    <br><br>
    <div>
        <% if(details.length>0){%>
            <table border="1">
                <tr>
                    <th>S No.</th>
                    <th>Job Role</th>
                    <th>Compony Name</th>
                    <th>Stipend</th>
                    <th>Details</th>
                </tr>    
            <% details.forEach((element,index)=>{%>
                <tr>
                    <td><%= index+1 %></td>
                    <td><%= element.role %></td>
                    <td><%= element.company %></td>
                    <td><%= element.CTC %></td>
                    <td><a href=<%= element.link %>>Link</a></td>
                </tr>
            <% }) %>
            </table>
        <% } %>
    </div>
</body>
</html>